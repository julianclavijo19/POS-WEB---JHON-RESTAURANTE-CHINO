# Variables de la aplicación
# Producción (Railway):
NEXTAUTH_URL=https://el-dragon-del-tarra.up.railway.app
# Local: NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=tu-secreto-super-seguro-cambiar-en-produccion

# Base de datos PostgreSQL
# Para desarrollo local:
DATABASE_URL="postgresql://usuario:password@localhost:5432/comandas_digitales?schema=public"

# Para Railway/Postgres (se configura en el panel):
# POSTGRES_PRISMA_URL=
# POSTGRES_URL_NON_POOLING=

# Configuración de impresora térmica (opcional, para print-server)
PRINTER_IP=192.168.1.100
PRINTER_PORT=9100

# URL del servidor de impresión (opcional; para impresión directa desde el navegador)
# NEXT_PUBLIC_PRINT_SERVER_URL=http://192.168.1.50:3001

# Polling: el print-server consulta esta app (Railway) para imprimir comandas
# Generar un secreto: openssl rand -base64 32
PRINT_POLLING_SECRET=
